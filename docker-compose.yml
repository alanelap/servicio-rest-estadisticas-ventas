services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: production
      FLASK_DEBUG: "0"
      HOST: 0.0.0.0
      PORT: "${PORT:-8000}"
      WORKERS: "${WORKERS:-2}"
      LOG_LEVEL: "${LOG_LEVEL:-INFO}"
      DATASET_PATH: data/ventas.csv
      PROCESSED_DATA_PATH: data/processed/ventas.parquet
      SUMMARY_CACHE_PATH: data/processed/statistics.json
      METADATA_PATH: data/processed/metadata.json
      QUALITY_REPORT_PATH: data/processed/quality_report.json
      STAT_TARGET_COLUMN: MONTO APLICADO
      AUTO_INGEST: "${AUTO_INGEST:-true}"
      MAX_REQUEST_BODY_BYTES: "${MAX_REQUEST_BODY_BYTES:-16384}"
      POLARS_MAX_THREADS: "${POLARS_MAX_THREADS:-4}"
    ports:
      # La publicación se limita al equipo local salvo decisión explícita del operador.
      - "${BIND_ADDRESS:-127.0.0.1}:${PORT:-8000}:${PORT:-8000}"
    volumes:
      # La fuente es inmutable para el contenedor; solo los derivados usan volumen escribible.
      - ./data/ventas.csv:/app/data/ventas.csv:ro
      - processed-data:/app/data/processed
    restart: unless-stopped
    init: true
    cap_drop:
      # La API no requiere capabilities de Linux para procesar ni servir datos.
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:${PORT:-8000}/health', timeout=3).read()"
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3

volumes:
  # Persiste el snapshot entre recreaciones sin versionar datos reales en Git.
  processed-data:
